import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

import { gsap } from 'gsap';

// Example GSAP animation
// gsap.to('.box', { duration: 3, rotation: 360 });

